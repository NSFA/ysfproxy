{
  "name": "ysfproxy",
  "version": "0.0.2",
  "description": "SailFish Proxy Platform App",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "release" : "electron-packager . ysfproxy --mac  --icon=./icon.icns --electron-version  --out=../ --overwrite=true --ignore=node_modules/electron --ignore=node_modules/.bin --ignore=.git --ignore=dist --prune=true"
  },
  "keywords": [
    "Electron",
    "proxy"
  ],
  "author": "GitHub",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "~1.6.2"
  }
}
